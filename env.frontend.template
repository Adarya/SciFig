# Frontend Environment Variables Template (React/Vite)
# Copy this to .env.local for local development
# These variables are compiled into the build and are PUBLIC
# Only include non-sensitive configuration here

# API Base URL - Railway will automatically set this in production
VITE_API_BASE_URL=http://localhost:8000/api/v1

# Application Info
VITE_APP_NAME=SciFig AI Platform
VITE_APP_VERSION=2.0.0

# Feature Flags
VITE_ENABLE_DEBUG=false
VITE_ENABLE_ANALYTICS=true

# UI Configuration  
VITE_MAX_FILE_SIZE=50
VITE_SUPPORTED_FORMATS=csv,xlsx,json

# External Services (public keys only - if needed)
# VITE_SUPABASE_URL=https://your-project-id.supabase.co
# VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ⚠️  SECURITY WARNING:
# NEVER put sensitive data here - these values are visible in the browser!
# No database URLs, secret keys, or private API keys!
